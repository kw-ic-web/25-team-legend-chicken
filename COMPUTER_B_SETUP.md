# 컴퓨터 B 설정 가이드

## 컴퓨터 A 정보
- IP 주소: `172.30.1.63`
- 프론트엔드 포트: `7777`
- 백엔드 포트: `8080`

## 컴퓨터 B에서 해야 할 일

### 방법 1: 브라우저로 직접 접속 (가장 간단)

1. **같은 네트워크에 연결 확인**
   - 컴퓨터 A와 같은 Wi-Fi 또는 이더넷 네트워크에 연결되어 있어야 합니다.

2. **브라우저에서 접속**
   ```
   http://172.30.1.63:7777
   ```

3. **완료!**
   - 자동으로 백엔드(`http://172.30.1.63:8080`)에 연결됩니다.

### 방법 2: 환경변수 설정 (더 안정적)

1. **프론트엔드 폴더로 이동**
   ```bash
   cd frontend
   ```

2. **`.env` 파일 생성**
   ```bash
   echo "VITE_BACKEND_API_KEY=http://172.30.1.63:8080" > .env
   ```

3. **프론트엔드 실행**
   ```bash
   npm run dev
   ```

4. **브라우저에서 접속**
   ```
   http://localhost:7777
   ```

### 방법 3: 쿼리 파라미터 사용

브라우저에서 접속:
```
http://172.30.1.63:7777?backendHost=172.30.1.63
```

## 문제 해결

### 연결이 안 되는 경우

1. **방화벽 확인**
   - 컴퓨터 A의 방화벽에서 포트 7777, 8080이 열려있는지 확인

2. **네트워크 확인**
   - 두 컴퓨터가 같은 네트워크에 있는지 확인
   - `ping 172.30.1.63` 명령어로 연결 확인

3. **서버 실행 확인**
   - 컴퓨터 A에서 백엔드와 프론트엔드가 실행 중인지 확인

### WebRTC가 작동하지 않는 경우

1. **브라우저 콘솔 확인**
   - F12를 눌러 개발자 도구 열기
   - Console 탭에서 오류 확인
   - Network 탭에서 Socket.io 연결 확인

2. **STUN 서버 확인**
   - 인터넷 연결이 필요합니다
   - Google STUN 서버를 사용 중입니다

