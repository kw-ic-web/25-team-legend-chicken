# 다른 컴퓨터에서 WebRTC 테스트 방법

## 문제 해결

### 1. 백엔드 서버 설정
- 백엔드 서버가 `0.0.0.0`으로 바인딩되어 모든 네트워크 인터페이스에서 접근 가능합니다.
- 포트: `8080`

### 2. 프론트엔드 설정
- `getBaseUrl()`이 자동으로 현재 호스트의 IP를 사용합니다.
- 다른 컴퓨터에서 접근하면 해당 컴퓨터의 IP로 자동 연결됩니다.

## 테스트 방법

### 컴퓨터 A (서버 실행 컴퓨터)

1. **IP 주소 확인**
```bash
# macOS/Linux
ifconfig | grep "inet " | grep -v 127.0.0.1

# Windows
ipconfig
```

예: `192.168.0.100`

2. **백엔드 서버 실행**
```bash
cd backend
npm start
# 또는
node server.js
```

3. **프론트엔드 실행**
```bash
cd frontend
npm run dev
```

4. **브라우저에서 접속**
- `http://localhost:7777` (같은 컴퓨터)
- 또는 `http://192.168.0.100:7777` (같은 컴퓨터에서도 가능)

### 컴퓨터 B (다른 컴퓨터)

1. **같은 네트워크에 연결 확인**
   - 컴퓨터 A와 같은 Wi-Fi 또는 이더넷 네트워크에 연결되어 있어야 합니다.

2. **브라우저에서 접속**
   - 방법 1: `http://192.168.0.100:7777` (컴퓨터 A의 IP 주소 사용)
   - 방법 2: `http://192.168.0.100:7777?backendHost=192.168.0.100` (백엔드 호스트 명시)

3. **환경변수 설정 (권장)**
   - 프론트엔드 루트에 `.env` 파일 생성:
   ```
   VITE_BACKEND_API_KEY=http://192.168.0.100:8080
   ```
   - 파일 생성 후 프론트엔드 서버 재시작 필요

## 문제 해결

### WebRTC가 작동하지 않는 경우

1. **방화벽 확인**
   - 컴퓨터 A의 방화벽에서 포트 8080과 7777이 열려있는지 확인
   - macOS: 시스템 설정 > 네트워크 > 방화벽
   - Windows: Windows Defender 방화벽

2. **네트워크 확인**
   - 두 컴퓨터가 같은 네트워크에 있는지 확인
   - 공용 Wi-Fi는 종종 클라이언트 간 통신을 차단합니다

3. **STUN 서버 확인**
   - WebRTC는 STUN 서버를 사용합니다 (Google STUN 서버 사용 중)
   - 인터넷 연결이 필요합니다

4. **브라우저 콘솔 확인**
   - F12를 눌러 개발자 도구 열기
   - Console 탭에서 WebRTC 관련 오류 확인
   - Network 탭에서 Socket.io 연결 확인

### Socket.io 연결 오류

1. **CORS 오류가 발생하는 경우**
   - 백엔드의 `CORS_ORIGIN` 환경변수를 `*`로 설정하거나
   - 프론트엔드의 IP를 명시적으로 추가

2. **연결이 안 되는 경우**
   - 컴퓨터 A의 IP 주소가 올바른지 확인
   - 백엔드 서버가 실행 중인지 확인
   - 포트가 차단되지 않았는지 확인

## 환경변수 설정 (고급)

### 백엔드 `.env`
```env
PORT=8080
CORS_ORIGIN=*
MONGODB_URI=your_mongodb_uri
```

### 프론트엔드 `.env`
```env
VITE_BACKEND_API_KEY=http://192.168.0.100:8080
```

## 주의사항

1. **동적 IP 주소**
   - 대부분의 네트워크에서 IP 주소는 동적으로 할당됩니다
   - IP가 변경되면 `.env` 파일을 업데이트해야 합니다

2. **보안**
   - 개발 환경에서만 `CORS_ORIGIN=*` 사용
   - 프로덕션에서는 특정 도메인만 허용

3. **NAT/방화벽**
   - 회사 네트워크나 공용 Wi-Fi는 NAT 뒤에 있을 수 있습니다
   - 이 경우 WebRTC가 작동하지 않을 수 있습니다
   - TURN 서버가 필요할 수 있습니다

